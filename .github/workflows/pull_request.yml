name: pull_request

on:
  workflow_dispatch:
  pull_request:
    branches:
      - master

jobs:
  checks:
    name: Checks
    permissions:
      contents: read
      checks: write
    uses: ./.github/workflows/checks.yml